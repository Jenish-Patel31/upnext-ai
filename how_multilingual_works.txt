// 1. User speaks in Hindi: "मैंने दोपहर के खाने पर 500 रुपये खर्च किए"

// 2. Voice-to-Text conversion
const hindiText = "मैंने दोपहर के खाने पर 500 रुपये खर्च किए";

// 3. AI Parsing with cultural context
const parsedExpense = await parseExpenseWithAI(hindiText, categories, 'hi-IN');

// 4. AI returns structured data
const aiResponse = {
  amount: 500,
  category: "Food",
  title: "Afternoon meal",
  date: "today",
  confidence: 0.99,
  language: "hi-IN",
  culturalContext: "Indian lunch context"
};

// 5. Store in database with multilingual metadata
const newExpense = new Expense({
  uid: user.uid,
  title: aiResponse.title, // English title for consistency
  amount: aiResponse.amount,
  category: aiResponse.category,
  date: parseDateIntelligently(aiResponse.date),
  // Multilingual metadata
  originalLanguage: 'hi-IN',
  originalText: hindiText, // Preserve original Hindi text
  culturalContext: aiResponse.culturalContext,
  confidence: aiResponse.confidence,
  languageCode: 'hi',
  parsedAt: new Date()
});

await newExpense.save();